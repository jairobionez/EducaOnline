apiVersion: apps/v1
kind: Deployment
metadata:
  name: pedidos-service
  namespace: educaonline
  labels:
    app: pedidos-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pedidos-service
  template:
    metadata:
      labels:
        app: pedidos-service
    spec:
      containers:
        - name: pedidos
          image: educaonline.pedidos.api:local
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: MessageQueueConnection__MessageBus
              value: "amqp://guest:guest@rabbitmq:5672"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

---

apiVersion: v1
kind: Service
metadata:
  name: pedidos-service
  namespace: educaonline
spec:
  selector:
    app: pedidos-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
